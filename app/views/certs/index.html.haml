%h1 Listing certs

%table
  %tr
    %th
    %th
    %th

  - @certs.each do |cert|
    %tr
      %td= link_to 'Show', cert
      %td= link_to 'Edit', edit_cert_path(cert)
      %td= link_to 'Destroy', cert, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Cert', new_cert_path
